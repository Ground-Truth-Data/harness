{
	"name": "@retreever/rapper",
	"version": "0.1.0",
	"type": "module",
	"engines": {
		"node": ">=22"
	},
	"_why": "THE THIN RAPPER. A SvelteKit config, an app shell, and the children — nothing else. Every script and dependency here must be traceable to something this project actually runs. Deps went 91 -> 43 -> 20 across the RAPPER strip; the ones that vanished were auth, payments, Postgres, express, an unused UI kit, Capacitor (no capacitor.config here), Prisma (no schema here), Tailwind/PostCSS (no @tailwind directive anywhere in src/) and the whole eslint/prettier set (no lint config file exists, so `npm run lint` could never have run). See RAPPER.md.",
	"_why_tailwind_correction": "⚠️ CORRECTED 27 Aug 2026 — `_why` ABOVE IS WRONG ABOUT TAILWIND/POSTCSS. It lists them among the RAPPER strip's casualties, justified with \"no @tailwind directive anywhere in src/\". That sentence is TRUE AND MEANINGLESS: Tailwind v4 — the version installed here — replaced `@tailwind base;` with `@import 'tailwindcss'`. The check looked for v3 syntax in a v4 project, found nothing, and read absence as proof of disuse. It is the same shape as RULE 7's stripComment() cutting each line at `//`, which is also what `https://` contains: a check that CANNOT FIRE, reported as a pass. MEASURED 27 Aug 2026, all still true: this file declares tailwindcss, @tailwindcss/postcss, postcss, tailwind-merge and tailwindcss-animate; src/lib/core/base.css:5 and retreeved/app.css:5 both `@import 'tailwindcss'`; ReTreever_who_what/lib/cn.ts calls twMerge(clsx(...)); postcss.config.js loads @tailwindcss/postcss. gitEr/packTest.sh built all four children green from the tarball, which it could not do against a missing `tailwindcss`. THE COUNT IS ALSO SUSPECT: `_why` says deps went 91 -> 43 -> 20, but dependencies + devDependencies here now total more than 20, so either the strip was partly reverted or the number was never re-measured — do not quote it without counting. WHAT IS STILL TRUE: the strip itself happened, and the OTHER casualties named (auth, payments, Postgres, express, Capacitor, Prisma, the eslint/prettier set) are genuinely gone. Only the Tailwind/PostCSS clause is false. KEPT rather than edited into `_why`, because the wrong sentence is the useful part: anyone who greps `@tailwind`, finds nothing, and concludes 'unused' is about to delete a live dependency. See the matching correction in retreeved/app.tokens.css.",
	"_why_prepublishOnly": "A PUBLISH THAT CANNOT INSTALL MUST NOT LEAVE THIS MACHINE. gitEr/packTest.sh packs this package, unpacks the tarball into a temp dir with no workspace above it, runs `npm create`, then installs and builds EVERY child, and finally checks that spread-children.mjs can lay all of them down. The tarball is the only artifact a stranger ever sees, and until 27 Aug 2026 nothing tested it: the create path had run once, by hand, on one machine, and had accumulated four defects nobody had noticed — including getCache_OfflineMap being unbuildable from the package outright. WHY prepublishOnly AND NOT prepack: packTest.sh itself runs `npm pack`, so hanging it off prepack would recurse. prepublishOnly runs first and to completion, and its inner pack cleans up after itself before the outer one starts. It also runs ONLY on publish, never on a plain `npm pack` or an install, which is what keeps it off contributors' machines. COST: several minutes and roughly 2GB of free disk, because every child gets a real node_modules. That is the price of knowing. `npm publish --ignore-scripts` skips it, and doing so means publishing something no one has proven installs.",
	"scripts": {
		"dev": "vite dev --port 5174",
		"prebuild": "svelte-kit sync",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"test:unit": "vitest",
		"prepack": "node ../rapper_director/bin/vendor-children.mjs",
		"postpack": "node ../rapper_director/bin/clean-children.mjs",
		"prepublishOnly": "bash ../gitEr/packTest.sh"
	},
	"dependencies": {
		"@mapbox/vector-tile": "^3.0.0",
		"@sentry/sveltekit": "10.43.0",
		"@tmcw/togeojson": "^7.1.2",
		"@turf/turf": "^7.2.0",
		"clsx": "^2.1.1",
		"fflate": "^0.8.2",
		"geojson": "^0.5.0",
		"mapbox-gl": "^3.23.1",
		"mapbox-gl-opacity": "^1.2.0",
		"maplibre-gl": "^5.13.0",
		"pbf": "^4.0.1",
		"tailwind-merge": "^3.4.0"
	},
	"devDependencies": {
		"@capacitor/core": "^8.3.0",
		"@capacitor/filesystem": "^8.1.2",
		"@capacitor/geolocation": "^8.1.0",
		"@capacitor/network": "^8.0.1",
		"@capacitor/share": "^8.0.1",
		"@sveltejs/adapter-static": "^3.0.10",
		"@sveltejs/adapter-vercel": "^6.2.0",
		"@sveltejs/kit": "^2.16.0",
		"@sveltejs/vite-plugin-svelte": "^4.0.4",
		"@tailwindcss/postcss": "^4.3.3",
		"@types/node": "^22.19.1",
		"fake-indexeddb": "^6.0.0",
		"postcss": "^8.5.26",
		"svelte": "^5.19.0",
		"svelte-check": "^4.1.4",
		"svelte-sonner": "^1.0.6",
		"tailwindcss": "^4.3.3",
		"tailwindcss-animate": "^1.0.7",
		"typescript": "^5.7.3",
		"vite": "^5.4.12",
		"vitest": "^2.1.9"
	},
	"description": "A SvelteKit rapper that runs any one of ReTreever and Get Cache's component repos on its own.",
	"license": "AGPL-3.0-or-later",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Ground-Truth-Data/rapper.git"
	},
	"homepage": "https://github.com/Ground-Truth-Data/rapper#readme",
	"bugs": {
		"url": "https://github.com/Ground-Truth-Data/rapper/issues"
	},
	"keywords": [
		"sveltekit",
		"reforestation",
		"offline-map",
		"retreever",
		"getcache"
	],
	"bin": {
		"create-rapper": "./_director/bin/create.mjs"
	},
	"files": [
		"src/",
		"retreeved/",
		"_children/",
		"_director/",
		"svelte.config.js",
		"vite.config.ts",
		"tsconfig.json",
		"postcss.config.js",
		"README.md",
		"CONTRIBUTING.md",
		"LICENSE"
	],
	"publishConfig": {
		"access": "public"
	}
}
